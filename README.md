# RoValid
Checks Roblox username availability without a token or paid proxies. Screens 200 names per request via the bulk endpoint, then confirms survivors against the signup validator to catch censored and reserved names. Async Python + aiohttp + rich TUI
